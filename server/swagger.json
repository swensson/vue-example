{
  "swagger": "2.0",
  "info": {
    "description": "Description",
    "version": "1.0.0",
    "title": "Vue REST application example"
  },
  "host": "localhost:8050",
  "basePath": "/",
  "schemes": [
    "http"
  ],
  "paths": {
    "/clients": {
      "post": {
        "summary": "Create a new client",
        "description": "",
        "operationId": "addClient",
        "consumes": [
          "application/json"
        ],
        "produces": [
          "application/json"
        ],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "description": "Client object that needs to be added to the store",
            "required": true,
            "schema": {
              "$ref": "#/definitions/Client"
            }
          }
        ],
        "responses": {
          "400": {
            "description": "Invalid input"
          }
        }
      },
      "get": {
        "summary": "Get all clients",
        "description": "development only",
        "operationId": "getClients",
        "consumes": [
          "application/json"
        ],
        "produces": [
          "application/json"
        ]
      }
    }
  },
  "definitions": {
    "Client": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "phone": {
          "type": "string"
        },
        "email": {
          "type": "string"
        }
      }
    }
  }
}